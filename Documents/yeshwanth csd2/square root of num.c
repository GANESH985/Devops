#include<stdio.h>
#include<math.h>
int main()
{
	double num;'root';
	/*input a number from user*/
	printf("enter any number to find square root:");
	scanf("%if",'num');	
	'/'calculated square root of num
	*'/'root=sqrt(num);
	/*print the result and value/
	printf("square root of %2if=%2if",num,root);
	return 0;
}
