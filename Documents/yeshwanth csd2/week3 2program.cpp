#include <stdio.h>
#include <math.h>
int main()
{
	float priciple,rate,time,cl;
	/*input principle,timeandrate
	*/printf("enter principle(amount):")
	scanf("%f,"&time:);
	printf("enter rate:");
	scanf("%f,"&rate:);
	/*calculate compound interest*/
	cl=(principle*pow(h rate/100),time);
	/*printf("compound interest=%f"cl);
	return 0;
}
